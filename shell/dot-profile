src() { [[ -r "$1" ]] && source "$1"; }

export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:/usr/local/lib64/pkgconfig
export XDG_CONFIG_HOME="$HOME/.config"

alias c="cargo"
alias g="git"
alias v="nvim"
alias vim="nvim"

src "$HOME/.profile-secret"
src "$HOME/.cargo/env"
